import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hospital.quality',
  appName: 'نظام إدارة الجودة',
  webDir: 'dist',
};

export default config;
